<!--pages/homepage/homepage.wxml-->
<view>
  <view wx:for="{{books}}" wx:key="{{index}}">
    <book book='{{(item)}}' bindtap='tobookdetail' data-bookid='{{item.id}}'></book>
  </view>
  <view>
    <button bindtap="tosell">卖书</button>
    <button bindtap="tocenter">个人中心</button>
  </view>
</view>
